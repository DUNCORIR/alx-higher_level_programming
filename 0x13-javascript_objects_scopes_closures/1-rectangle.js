#!/usr/bin/node

// The script for a class rectangle that defines Rectangle.
class Rectangle {
  constructor (w, h) {
    this.width = w; // Initialize width with value w
    this.height = h; // Initialize height with value h
  }
}

module.exports = Rectangle;
